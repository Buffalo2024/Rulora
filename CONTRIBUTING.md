# 参与贡献

感谢你愿意参与 Rulora。这个仓库希望沉淀可复用的 Hybrid 机制，而不是收集未经整理的
Prompt 或私有业务代码。

## 适合贡献什么

- 状态机、Repository、Provider、Channel 或 Quality Gate 的通用能力；
- Agent Manifest、任务、证据、交付物和 QA 等公共契约；
- 能证明“模型负责开放任务、程序负责确定任务”的最小教学示例；
- 状态转换、错误恢复、证据绑定和输出验证测试；
- 中英文文档、示例、安装体验和安全改进。

## 不应提交什么

- API Key、用户原始对话、支付记录或生产日志；
- 未获得授权的图片、字体、Prompt、数据集和第三方资料；
- 只能服务一个私有业务、无法抽象成通用契约的代码；
- 允许模型绕过程序状态机直接修改业务状态的实现。

## Agent 项目与场景包

解决具体用户任务的 Agent 应作为独立项目维护，并在达到可运行、可验证和可复现标准后
申请加入 Rulora 官方索引。共享同一执行底座、只在周期、行业、来源或模板上存在差异的
能力，应作为一个 Agent 内的可路由场景包，不应拆成多个仓库。

请不要只为预告创建空 Agent 仓库。新 Agent 至少应提供安装说明、示例输入、可复现输出、
边界说明、自动化测试和机器可读 QA 结果。

## 开发流程

1. Fork 仓库并从 `main` 创建分支；
2. 修改代码并为状态转换添加测试；
3. 运行 `npm test`、三个案例和 `npm run release:check`；
4. 确认没有密钥、本地绝对路径、真实用户数据或无权分发的资产；
5. 提交 Pull Request，说明问题、Hybrid 职责划分和验证结果。

新增案例必须包含独立 README，并让读者在 30 秒内知道：解决什么问题、模型做什么、
程序做什么、需要什么输入、产出什么、边界在哪里。

提交行为应遵守 [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)。
